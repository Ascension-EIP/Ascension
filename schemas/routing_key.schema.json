{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RoutingKey",
  "type": "string",
  "enum": [
    "pose.detect.requested",
    "pose.detect.completed",
    "pose.advice.requested",
    "pose.detect.status",
    "pose.advice.status"
  ]
}
