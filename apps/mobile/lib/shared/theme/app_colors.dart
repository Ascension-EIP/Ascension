import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF00B5D3);
  static const primaryDark = Color(0xFF0098B5);
  static const accent = Color(0xFF00B5D3);

  // Background
  static const backgroundDark = Color(0xFF0D1B2A);
  static const surfaceDark = Color(0xFF0E1626);
  static const cardDark = Color(0xFF162032);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0x8AFFFFFF);

  // Status
  static const info = Color(0xFF80D8FF);
  static const success = Color(0xFF69F0AE);
  static const warning = Color(0xFFFFAB40);
  static const danger = Color(0xFFFF5252);
  static const error = Color(0xFFFF5252);
}
